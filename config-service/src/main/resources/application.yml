server:
  port: 8888
  
spring:
  cloud:
    config:
      server: 
        git:
          uri: https://github.com/wetdawgs/config-data.git
          search-paths: api
          default-label: "main"
          
  security:
    user:
      name: root
      password: adminadmin
